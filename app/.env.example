# Copy to `.env` (Bun auto-loads it). See src/config/env.ts.
NODE_ENV=development
PORT=3000

# Local dev DB (matches docker/docker-compose.dev.yml).
# When running the app INSIDE compose, the host is `postgres` instead of localhost.
DATABASE_URL=postgres://renovel:renovel@localhost:5432/renovel
